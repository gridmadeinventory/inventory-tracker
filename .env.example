PORT=3000
NODE_ENV=development

# TCGPlayer API credentials. Both are required for product search and live
# pricing — the API is OAuth-only and rejects unauthenticated calls.
# Request access at https://developer.tcgplayer.com/
TCGPLAYER_PUBLIC_KEY=
TCGPLAYER_PRIVATE_KEY=
